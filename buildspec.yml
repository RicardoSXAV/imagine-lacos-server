version: 0.2

phases:
  pre_build:
    commands:
      - echo Installing packages
      - npm install
    build:
      - echo Creating build
      - npm run build
    post_build:
      - echo Prepare artifact
      - npm run dist
artifacts:
  base-directory: "."
  files:
    - "**/*"
